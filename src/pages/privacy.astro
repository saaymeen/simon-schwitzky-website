---
export const prerender = false;

import { getLocalizedPageURL } from 'utils/i18n';

const redirectURL = getLocalizedPageURL(Astro.preferredLocale, Astro.url);
return Astro.redirect(redirectURL, 302);
---
